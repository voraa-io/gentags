# Phase 3 docs

This folder holds **run logs and findings** for Phase 3 experiments. Use it to see what we ran, what we got, and whether we changed anything.

**State-Gini status:** Preflight (orthogonality + τ sweep) is done. The **full State-Gini experiment has not been run yet** — preflight was only to set up and validate before we run the real thing.

- **state_gini_preflight_runs.md** — Preflight runs: results and decisions (change anchors? lock τ?). Full State-Gini run log will go here (or a separate doc) when we run it.
